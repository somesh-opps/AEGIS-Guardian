from __future__ import annotations

# =============================================================
# AEGIS AI Brain — Central Configuration TEMPLATE
# =============================================================
# HOW TO USE:
#   1. Copy this file:  cp config.py.example config.py
#   2. Fill in your real values in config.py
#   3. config.py is gitignored — it will NEVER be committed
# =============================================================

# ── MongoDB ──────────────────────────────────────────────────
MONGO_URI = "mongodb+srv://<username>:<password>@<cluster>.mongodb.net/"
DB_NAME   = "aegis_db"

# ── Gemini API Key Pool ───────────────────────────────────────
# Add as many keys as you have for load balancing / rate-limit resilience
GEMINI_API_KEY_1  = "your_gemini_api_key_here"
GEMINI_API_KEY_2  = "your_gemini_api_key_here"
# ... add more as needed

GEMINI_API_KEYS: list[str] = [
    GEMINI_API_KEY_1,
    GEMINI_API_KEY_2,
]

GEMINI_MODEL = "gemini-1.5-flash"

# ── Ollama Fallback ───────────────────────────────────────────
OLLAMA_BASE_URL = "http://localhost:11434"
OLLAMA_MODEL    = "qwen2.5:0.5b"

# ── YOLO Vision Service ───────────────────────────────────────
VISION_PUSH_SECRET = "change_me_to_a_strong_secret"   # must match aegis-vision config.py

# ── Firebase Realtime Database ─────────────────────────────────
FIREBASE_RTDB_URL              = "https://<your-project>.firebasedatabase.app/.json"
FIREBASE_POLL_INTERVAL_SECONDS = 3.0

# ── Node → Building/Floor Mapping ────────────────────────────
NODE_BUILDING_MAP: dict[str, dict] = {
    "A1": {"building": "Academic Block", "floor": 1},
    "B1": {"building": "Research Block", "floor": 1},
}

# ── Sensor Fusion Thresholds ──────────────────────────────────
THRESHOLDS = {
    "smoke": {
        "LOW":    (0,   149),
        "MEDIUM": (150, 299),
        "HIGH":   (300, 9999),
    },
    "gas": {
        "LOW":    (0,   149),
        "MEDIUM": (150, 299),
        "HIGH":   (300, 9999),
    },
    "temperature": {
        "NORMAL":   (0,  39),
        "ELEVATED": (40, 49),
        "HIGH":     (50, 999),
    },
    "humidity": {
        "LOW":    (0,  39),
        "NORMAL": (40, 79),
        "HIGH":   (80, 100),
    },
    "current_abnormal_threshold": 10.0,   # Amps
}

# ── Conversation History ──────────────────────────────────────
CHAT_HISTORY_LIMIT = 10   # last N turns sent to AI

# ── AI Retry ─────────────────────────────────────────────────
AI_MAX_RETRIES = 2
